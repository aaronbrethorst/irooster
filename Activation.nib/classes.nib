{
    IBClasses = (
        {
            ACTIONS = {cancel = id; registerApp = id; }; 
            CLASS = ActivationController; 
            LANGUAGE = ObjC; 
            OUTLETS = {txtName = NSTextField; txtSerialNumber = NSTextField; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}